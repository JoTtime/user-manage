[build]
builder = "maven"
buildCommand = "mvn clean package -DskipTests"

[deploy]
startCommand = "java -jar target/Harvest-0.0.1-SNAPSHOT.jar"

[healthcheck]
path = "/actuator/health"
port = 8080
initialDelay = 30
timeout = 5